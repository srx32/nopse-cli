{
  "name": "project-setup-cli",
  "version": "1.0.0",
  "description": "",
  "main": "main/index.ts",
  "bin": {
    "no-name": "dist/index.js"
  },
  "scripts": {
    "start": "ts-node main/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "serve": "npx .",
    "deploy": "npm run build && npm run serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
